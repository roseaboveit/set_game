import Backbone from 'backbone';

const Card = Backbone.Model.extend({
  initialize: function() {
    // color: hash['color'],
    // number: hash['number'],
    // shading: hash['shading'],
    // shape: hash['shape']
  }
})

export default Card;
